import 'package:flutter/material.dart';

class PollVoteScreen extends StatelessWidget {
  static const String id = 'poll_screen';

  final int pollId;

  PollVoteScreen({@required this.pollId});
  
  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}