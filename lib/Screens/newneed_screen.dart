import 'package:flutter/material.dart';

class NewNeedScreen extends StatelessWidget {
  
  static const String id = 'newneed_screen';
  
  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}